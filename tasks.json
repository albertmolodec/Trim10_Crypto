{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build",
            "type": "shell",
            // "command": "g++ -g Main.cpp -o RSA-build",
            //"command": "g++ -g ElGamal.cpp -o ElGamal-build",
            "command": "g++ -g ElGamalPlus.cpp -o ElGamalPlus-build",
            // "command": "g++ -g Ksyu.cpp -o KSYU-build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}